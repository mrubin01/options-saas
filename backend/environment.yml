channels:
  - conda-forge        # Preferred community-maintained packages
  - defaults

dependencies:
  - python=3.11
  - sqlalchemy
  - sqlite 
  - numpy
  - pandas
  - pip                # Required if using pip section
  - pip:
      - requests
      - yfinance==0.2.59
      - curl-cffi==0.10.0
      - fastapi==0.95.2
      - uvicorn==0.22.0
      - pydantic==1.10.7
      - python-dotenv==1.0.0
