channels:
  - conda-forge        # Preferred community-maintained packages
  - defaults

dependencies:
  - python=3.11
  - numpy
  - pandas
  - pip                # Required if using pip section
  - pip:
      - requests
      - yfinance==0.2.59
      - curl-cffi==0.10.0
